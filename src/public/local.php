<?php 
/**
 * Path to the public_html directory
 */
// define( 'PUBLIC_HTML', dirname(dirname(dirname(__DIR__))) );

/**
 * Path to a directory outside public_html that will contain the application's 
 * "private" files. 
 */
// define( 'PRIVATE_HTML', PUBLIC_HTML );

/**
 * Application's public directory
 */
// define( 'PUBLIC_APP_PATH',  PUBLIC_HTML . DIRECTORY_SEPARATOR . $__['appName'] );

/**
 * Application's private directory.
 */
// define( 'PRIVATE_APP_PATH', PRIVATE_HTML . DIRECTORY_SEPARATOR . $__['appName'] );

/**
 * Directory where the application's public files are located, this is where index.php 
 * and .htaccess are.
 */
// define( 'APP_PUBLIC_PATH',  realpath(PUBLIC_APP_PATH . '/src/public') );

/**
 * Root directory for all source files. 
 */
// define( 'APP_SRC_PATH',     PRIVATE_APP_PATH . DIRECTORY_SEPARATOR . 'src' );
// define( 'APP_PATH',         APP_SRC_PATH . DIRECTORY_SEPARATOR . 'app' );
// define( 'VENDOR_PATH',      APP_SRC_PATH . DIRECTORY_SEPARATOR . 'vendor' );
// define( 'ROUTES_PATH',      APP_PATH . DIRECTORY_SEPARATOR . 'routes' );

/**
 * Path to where configuration files are located.
 */
// define( 'CONFIG_PATH',      APP_PATH . DIRECTORY_SEPARATOR . 'config' );

/**
 * Path to where log files are wrtten.
 */
// define( 'LOG_PATH',         PRIVATE_APP_PATH . DIRECTORY_SEPARATOR . 'log' );
